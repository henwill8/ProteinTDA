[build-system]
requires = ["setuptools>=61.0", "wheel", "numpy", "ninja"]
build-backend = "setuptools.build_meta"

[project]
name = "vpd"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "torch>=2.3.0",
    "numpy",
]
